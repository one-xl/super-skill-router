# Skill Health Check

- status: warn
- checked scope: backend category
- findings:
  - `backend/SUBCATEGORY_INDEX.md` references `database-migration`, but `backend/database-migration/SKILL_TAG.md` is missing.
- suggested fixes:
  - Add `backend/database-migration/SKILL_TAG.md`, or remove the stale index entry.

