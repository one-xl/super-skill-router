# Example User Task

用户任务：

```text
请帮我给一个 Web 应用新增登录表单，并说明后端需要提供哪些 API。
```

期望路由：

- 主 Skill：`frontend/web-frontend`
- 辅助 Skill：`backend/api-backend`

原因：

- “Web 应用”“登录表单”命中 `frontend`。
- “后端需要提供哪些 API”命中 `backend`。
- 任务需要跨领域组合，但前端交付是主目标。

