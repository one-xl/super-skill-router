# Agent Memory Skill Tag

## 标签

记忆、Memory、上下文、Context、持久化、Session、历史、压缩、RAG、claude-mem、会话、记录

## 适用场景

- 需要保留或查询前几次会话的上下文与项目演进历史。
- 进行上下文压缩或查询历史运行摘要。
- 检索往期开发决策或观察。

## 读取完整 Skill 的条件

- 需要执行 `claude-mem` 的核心指令（如 `search`, `status` 等）。
- 需手动配置记忆过滤策略或配置隐私 `private` 标记。
- 需要深入理解跨会话记忆的结构或更新记忆快照。
