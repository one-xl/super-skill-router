# Deployment Category Tag

## 分类用途

处理部署、运行环境、反向代理、HTTPS、域名、容器和静态资源发布任务。

## 命中条件

- 用户要求部署、上线或排查线上访问问题。
- 用户提到 Nginx、Docker、PM2、HTTPS、域名、反向代理或静态资源。
- 用户任务涉及服务运行、端口转发或访问配置。

## 排除条件

- 只涉及业务接口实现时，优先选择 `backend`。
- 只涉及浏览器页面样式时，优先选择 `frontend`。

## 子类索引

读取 `deployment/SUBCATEGORY_INDEX.md`。

