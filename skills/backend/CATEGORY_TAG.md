# Backend Category Tag

## 分类用途

处理服务端接口、认证授权、业务逻辑、数据处理和后台任务。

## 命中条件

- 用户要求新增或修改 API。
- 用户提到鉴权、登录、Session、Token、上传、下载、队列或服务端业务逻辑。
- 用户任务需要调整数据处理流程或后端模块。

## 排除条件

- 只涉及页面样式和浏览器交互时，优先选择 `frontend`。
- 只涉及上线、反向代理或运行环境时，优先选择 `deployment`。

## 子类索引

读取 `backend/SUBCATEGORY_INDEX.md`。

