# Super Skill Router 修复建议

复查时间：2026-07-02

## 当前状态

当前项目已经完成主结构优化：

- `node scripts\doctor.js` PASS，且 0 error、0 warning。
- 5 个 route smoke 样例均通过，输出 JSON 可解析，路径真实存在。
- `skills-manifest.json` 共 `141` 条记录，无重复 id、无无效路径、无 `public` visibility 泄露、无缺失字段。
- `SKILL.md` frontmatter 已标准化，项目内与全局安装目录同步一致。

剩余问题主要集中在文档可读性和最终提交卫生。

## P0：重写核心文档为干净 UTF-8

### 问题

核心文档仍有明显 mojibake 信号：

- `SKILL.md` 49
- `AGENTS.md` 34
- `README.md` 23
- `router/CATEGORY_INDEX.md` 72
- `router/SKILL_POLICY.md` 48
- `router/ROUTER.md` 38
- `docs/zh-usage.md` 39
- `CONTRIBUTING.md` 29

当前这些文档虽然能看出结构，但中文阅读体验已经明显劣化。

### 建议

优先重写这些文件的正文为干净 UTF-8，保持短小、明确、可执行：

1. `SKILL.md`
2. `AGENTS.md`
3. `README.md`
4. `router/CATEGORY_INDEX.md`
5. `router/SKILL_POLICY.md`
6. `router/ROUTER.md`
7. `docs/zh-usage.md`
8. `CONTRIBUTING.md`

### 验收

```powershell
$files = @(
  'SKILL.md',
  'AGENTS.md',
  'README.md',
  'router\CATEGORY_INDEX.md',
  'router\SKILL_POLICY.md',
  'router\ROUTER.md',
  'docs\zh-usage.md',
  'CONTRIBUTING.md'
)
foreach ($f in $files) {
  $txt = Get-Content -Raw -LiteralPath $f
  $signals = ([regex]::Matches($txt, '锛|銆|鐨|璺|褰|涓')).Count
  [pscustomobject]@{ file = $f; mojibakeSignals = $signals }
}
```

预期：这些核心文档的 mojibake 信号降到 0，或只剩少量可解释的历史说明。

## P1：修正 router/ROUTER.md 的旧 audit 命令

### 问题

`router/ROUTER.md` 仍包含：

```text
node <SUPER_SKILL_ROOT>/../scripts/route.js --audit
node <SUPER_SKILL_ROOT>/../scripts/route.js --audit --update
```

当前结构中 `SUPER_SKILL_ROOT` 已是仓库根目录，不应再使用 `../scripts`。

### 建议

改为：

```text
node <SUPER_SKILL_ROOT>/scripts/route.js --audit
node <SUPER_SKILL_ROOT>/scripts/route.js --audit --update
```

### 验收

```powershell
rg -n "\.\./scripts/route\.js" router\ROUTER.md
```

预期：无输出。

## P1：修正 docs/zh-usage.md 的旧示例

### 问题

`docs/zh-usage.md` 仍残留旧示例：

```text
BUSINESS_SKILL_ROOT = skills
```

这会误导后续维护者理解当前目录结构。

### 建议

将普通使用说明替换为当前结构：

```text
SUPER_SKILL_ROOT = .
BUSINESS_SKILL_ROOT = internal-skills
入口文件 = SKILL.md
```

### 验收

```powershell
rg -n "BUSINESS_SKILL_ROOT = skills|SUPER_SKILL_ROOT = skills|internal-skills[/\\]_super-skill" docs README.md AGENTS.md CONTRIBUTING.md
```

预期：除历史说明外，不再出现旧结构。

## P1：增强 doctor.js 的静态覆盖

### 问题

当前 `doctor.js` 已能检查 manifest、route smoke 和部分旧路径，但还可以把文档质量问题纳入正式检查，让后续回退更早暴露。

### 建议

增加这几类检查：

- 核心文档 mojibake 信号统计。
- `router/ROUTER.md` 中禁止 `../scripts/route.js`。
- 非历史文档中禁止 `BUSINESS_SKILL_ROOT = skills`、`SUPER_SKILL_ROOT = skills`。
- 继续保留 route smoke 路径存在性检查。

### 验收

```powershell
node scripts\doctor.js
```

预期：未修复上述问题时能报 warning；修复后继续 PASS 且 0 warning。

## P2：提交前整理工作树

### 问题

当前迁移仍然是一个大工作树：

- 旧 `skills/` 大量删除。
- 新 `internal-skills/`、`router/`、`scripts/`、`skills-manifest.json` 新增。
- `修复建议.md`、`修改建议.md` 同时存在。

### 建议

提交前明确分组：

- 结构迁移：旧 `skills/` 删除，新 `internal-skills/` 新增。
- Router：`router/`、`scripts/route.js`、`scripts/doctor.js`、`skills-manifest.json`。
- 文档：`SKILL.md`、`AGENTS.md`、`README.md`、`docs/`、`CONTRIBUTING.md`、`examples/`。
- 不提交运行时产物：`.codegraph/`、`*.pid`、缓存文件。

### 验收

```powershell
git status --short
```

预期：每类变更都能解释，没有无关运行产物混入。

## 最终验收命令

```powershell
node scripts\doctor.js

$files = @(
  'SKILL.md',
  'AGENTS.md',
  'README.md',
  'router\CATEGORY_INDEX.md',
  'router\SKILL_POLICY.md',
  'router\ROUTER.md',
  'docs\zh-usage.md',
  'CONTRIBUTING.md'
)
foreach ($f in $files) {
  $txt = Get-Content -Raw -LiteralPath $f
  $signals = ([regex]::Matches($txt, '锛|銆|鐨|璺|褰|涓')).Count
  [pscustomobject]@{ file = $f; mojibakeSignals = $signals }
}

rg -n "\.\./scripts/route\.js|BUSINESS_SKILL_ROOT = skills|SUPER_SKILL_ROOT = skills|internal-skills[/\\]_super-skill" -S router docs README.md AGENTS.md CONTRIBUTING.md

git status --short
```

最终目标：

- 功能继续 PASS。
- 核心文档无乱码。
- 旧结构残留只出现在明确的历史说明中。
- 工作树干净、可提交、可审查。
