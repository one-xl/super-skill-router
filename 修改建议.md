# Super Skill Router 修改建议

## 总体结论

当前项目已经完成 `ROUTER_OPTIMIZATION_PROPOSAL.md` 中的大部分结构性改造：

- 根目录只保留一个公开入口 `SKILL.md`。
- `internal-skills/` 下的子 Skill 已基本改为 `INSTRUCTION.md`。
- 已新增 `skills-manifest.json`，并由 `route.js` 输出结构化 JSON。
- 已新增 `doctor.js`，健康检查当前为 PASS。

但项目还没有完全收尾，主要剩余问题集中在旧路径文档、触发词冲突和路由精度三类。

## P0：修复旧入口路径文档

### 问题

`AGENTS.md` 和 `README.md` 仍然引用旧入口：

```text
internal-skills/_super-skill/INSTRUCTION.md
```

但当前仓库中该路径不存在。实际结构已经迁移为：

```text
SKILL.md
router/CATEGORY_INDEX.md
router/SKILL_RANKINGS.md
router/ROUTING_CONFIDENCE.md
router/SKILL_POLICY.md
```

### 建议

更新所有面向 Agent 的入口说明：

- 默认入口改为根目录 `SKILL.md`。
- 路由索引改为 `router/CATEGORY_INDEX.md`。
- 排行、策略、置信度文档统一使用 `router/` 下路径。
- 删除或改写 `internal-skills/_super-skill/` 相关描述。

### 验收

```powershell
Select-String -Path *.md,docs\*.md -Pattern 'internal-skills/_super-skill','internal-skills\\_super-skill'
```

预期：没有旧入口路径残留，除非是在明确的迁移历史说明中出现。

## P1：清理触发词高度重合

### 问题

`node scripts\doctor.js` 当前虽然 PASS，但仍报告 16 个 `HIGH_TRIGGER_COLLISION` warning，例如：

- `incremental-implementation` 与 `test-driven-development`
- `spec-driven-development` 与 `context-engineering`
- `brainstorm-experiments-existing` 与 `brainstorm-experiments-new`
- `identify-assumptions-existing` 与 `identify-assumptions-new`
- `hyperframes-core` 与 `remotion-to-hyperframes`
- `context-engineering` 与 `using-agent-skills`

这些冲突会降低 `route.js` 的选择稳定性。

### 建议

在 `skills-manifest.json` 中按以下原则拆分触发词：

- 保留每个 Skill 的强专属词。
- 删除 `use`、`when`、`create`、`generate`、`plan` 等泛词。
- 对成对 Skill 增加方向性词：
  - `existing` 类 Skill 使用 `existing`、`current`、`already`、`已有`。
  - `new` 类 Skill 使用 `new`、`from scratch`、`新建`、`空白`。
  - TDD 使用 `red-green-refactor`、`unit test first`、`先写测试`。
  - incremental implementation 使用 `small change`、`vertical slice`、`逐步实现`。

### 验收

```powershell
node scripts\doctor.js
```

预期：`HIGH_TRIGGER_COLLISION` warning 数量明显下降，最好为 0。

## P1：提升典型任务路由置信度

### 问题

测试命令：

```powershell
node scripts\route.js --query "修复 React 页面按钮样式 bug" --workspace "."
```

当前返回：

```json
{
  "primary": "web-frontend",
  "confidence": 0.35,
  "filesToRead": ["router/CATEGORY_INDEX.md"]
}
```

分类方向基本正确，但“修复 React 页面按钮样式 bug”应是较明确的 frontend + diagnose/karpathy 场景，不应低置信降级。

### 建议

调整 `scripts/route.js` 的评分逻辑：

- 对组合意图加分，例如 `修复/bug/error` + `React/CSS/button/page`。
- 前端 bug 场景可设为：
  - `primary = web-frontend`
  - `auxiliary = diagnose, code-indexing, karpathy-guidelines`
- 当 query 同时命中框架词、UI 词、修复词时，将 confidence 提升到 `>= 0.50` 或 `>= 0.80`。
- 避免只因跨 frontend 与 engineering 就压低置信度。

### 验收

```powershell
node scripts\route.js --query "修复 React 页面按钮样式 bug" --workspace "."
node scripts\route.js --query "review backend auth API security" --workspace "."
node scripts\route.js --query "给论文实验结果写 rebuttal" --workspace "."
```

预期：

- 输出仍为稳定 JSON。
- `filesToRead` 只包含必要 `INSTRUCTION.md` 或低置信时只含 `router/CATEGORY_INDEX.md`。
- 明确任务不应频繁落入 `confidence < 0.50`。

## P2：修复 proposal 自身的绝对路径示例

### 问题

`doctor.js` 报告：

```text
HARDCODED_ABSOLUTE_PATH: ROUTER_OPTIMIZATION_PROPOSAL.md
```

这是 proposal 中示例命令包含本机路径导致的。

### 建议

将示例中的本机绝对路径改为占位符或相对路径：

```powershell
Get-ChildItem -Recurse -Force "<repo-root>" -Filter SKILL.md |
  Select-Object FullName
```

或：

```powershell
Get-ChildItem -Recurse -Force . -Filter SKILL.md |
  Select-Object FullName
```

### 验收

```powershell
node scripts\doctor.js
```

预期：不再出现 `HARDCODED_ABSOLUTE_PATH` warning。

## P2：同步根入口文档与真实结构

### 问题

根目录 `SKILL.md` 中仍出现旧概念：

```text
SUPER_SKILL_ROOT = THIS_SKILL_DIR/skills
```

但当前仓库实际使用：

```text
internal-skills/
router/
skills-manifest.json
scripts/
```

### 建议

更新 `SKILL.md`：

- 将 `SUPER_SKILL_ROOT` 说明改为当前仓库根目录。
- 将 `BUSINESS_SKILL_ROOT` 说明改为 `internal-skills/`。
- 将 `CATEGORY_INDEX.md`、`SKILL_RANKINGS.md` 等路径统一改为 `router/`。
- 将 “读取目标 SKILL_TAG.md 后再读完整 SKILL.md” 改为 “读取目标 `INSTRUCTION.md`”。

### 验收

人工检查 `SKILL.md` 中不再指导 Agent 访问不存在的 `skills/` 或 `_super-skill/` 路径。

## 推荐实施顺序

1. 先修文档入口路径：`AGENTS.md`、`README.md`、`SKILL.md`。
2. 再修 proposal 自身绝对路径 warning。
3. 清理 `skills-manifest.json` 的触发词冲突。
4. 调整 `route.js` 对典型组合意图的置信度评分。
5. 最后运行完整验收命令。

## 最终验收命令

```powershell
Get-ChildItem -Recurse -Force . -Filter SKILL.md | Select-Object FullName
node scripts\doctor.js
node scripts\route.js --query "修复 React 页面按钮样式 bug" --workspace "."
```

最终目标：

- `SKILL.md` 只出现根目录一个。
- `doctor.js` PASS 且 warning 数量降到最低。
- 典型明确任务能输出较高置信度的结构化 JSON。
- 文档中的入口路径全部与当前真实目录结构一致。

---

## 第二轮复查追加建议

复查时间：2026-07-02

### 当前状态

本轮复查确认主体优化已经基本完成：

- 全仓库只剩根目录一个 `SKILL.md`。
- `skills-manifest.json` 当前共有 `141` 条 skill 记录。
- manifest 无重复 id、无无效 `instructionPath`、无 `public` visibility 泄露、无必填字段缺失。
- `node scripts\doctor.js` 当前为 PASS。
- 典型路由样例已经明显改善：
  - `修复 React 页面按钮样式 bug` -> `web-frontend`，confidence `0.92`。
  - `review backend auth API security` -> `security-and-hardening`，confidence `0.90`。
  - `给论文实验结果写 rebuttal` -> `scientific-research-skill`，confidence `0.95`。
  - `配置 nginx 反向代理和 https 证书` -> `nginx`，confidence `0.94375`。

但仍有几个值得继续优化的问题。

## P0：修复 SKILL.md frontmatter

### 问题

根目录 `SKILL.md` 文件开头出现重复 frontmatter：

```yaml
---
name: super-skill-router
description: ...
---
name: super-skill-router
description: ...---
```

第二段 `description` 和结束 `---` 还粘在同一行。这可能影响 Codex 对 Skill 元数据的解析。

### 建议

只保留一个标准 YAML frontmatter：

```yaml
---
name: super-skill-router
description: Global entrypoint for Super Skill Router. MUST BE TRIGGERED for ANY coding, development, bug fixing, refactoring, code review, UI/UX design, or architecture task.
---
```

frontmatter 后再开始正文标题。

### 验收

```powershell
Get-Content -LiteralPath SKILL.md -TotalCount 8
```

预期：

- 文件第 1 行是 `---`。
- 文件中只有一段 frontmatter。
- `description` 不与结束 `---` 粘连。

## P0：修复 route.js 中 hardcoded grammar-check 路径

### 问题

论文 rebuttal 路由当前返回：

```text
internal-skills/document/grammar-check/INSTRUCTION.md
```

但真实 manifest 路径是：

```text
internal-skills/engineering/planning/pm-skills/grammar-check/INSTRUCTION.md
```

因此 `filesToRead` 和 `absoluteAuxiliaryPaths` 中存在无效路径风险。

### 建议

不要在 `route.js` 中手写辅助 Skill 路径。所有 Skill 路径都应通过 manifest id lookup 获取：

```text
skill id -> manifest.skills[].instructionPath
```

尤其需要修复 `scientific-research-skill + grammar-check` 的特殊分支。

### 验收

```powershell
node scripts\route.js --query "给论文实验结果写 rebuttal" --workspace "."
```

预期：

- `filesToRead` 中的 `grammar-check` 路径指向真实存在文件。
- `absoluteAuxiliaryPaths` 中的路径也真实存在。

建议额外增加自动检查：

```powershell
node scripts\route.js --query "给论文实验结果写 rebuttal" --workspace "." |
  node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const fs=require('fs');const j=JSON.parse(s);for(const p of j.filesToRead||[]) if(!fs.existsSync(p)) throw new Error('missing '+p);console.log('ok')})"
```

## P1：统一 route.js 输出中的实际加载列表

### 问题

中置信度场景中，`filesToRead` 会裁剪辅助 Skill，但 `auxiliary` 和 `absoluteAuxiliaryPaths` 仍可能保留未实际加载的 Skill。

示例：

```powershell
node scripts\route.js --query "把需求拆成 GitHub issues" --workspace "."
```

当前表现：

- `filesToRead` 只包含 `to-issues` 和 `code-indexing`。
- `auxiliary` 仍包含 `code-indexing`、`karpathy-guidelines`。
- `absoluteAuxiliaryPaths` 也可能包含两个辅助路径。

这会让 Agent 不确定应该读取哪个列表。

### 建议

将输出拆成两个语义清晰的字段：

```json
{
  "recommendedAuxiliary": ["code-indexing", "karpathy-guidelines"],
  "loadedAuxiliary": ["code-indexing"],
  "filesToRead": []
}
```

或者保持当前字段不变，但让 `auxiliary`、`filesToRead`、`absoluteAuxiliaryPaths` 全部以实际加载结果为准。

### 验收

```powershell
node scripts\route.js --query "把需求拆成 GitHub issues" --workspace "."
```

预期：

- 若 `filesToRead` 只加载一个辅助 Skill，则 `absoluteAuxiliaryPaths` 也只返回该辅助 Skill。
- 字段语义不互相冲突。

## P1：为 doctor.js 增加 route 样例路径校验

### 问题

`doctor.js` 能检查 manifest 中的 `instructionPath`，但目前没有验证 `route.js` 特殊分支输出的路径是否存在。

因此 hardcoded 路径错误可能绕过 doctor。

### 建议

在 `doctor.js` 中新增一组 smoke route cases：

```json
[
  "修复 React 页面按钮样式 bug",
  "review backend auth API security",
  "给论文实验结果写 rebuttal",
  "把需求拆成 GitHub issues",
  "配置 nginx 反向代理和 https 证书"
]
```

对每个 route 输出执行：

- JSON 可解析。
- `filesToRead` 中每个非 router 文档路径都存在。
- `absoluteSkillPath` 存在。
- `absoluteAuxiliaryPaths` 中每个路径都存在。
- `primary` 能在 manifest 中找到。

### 验收

```powershell
node scripts\doctor.js
```

预期：doctor 能捕获 route 输出中的坏路径，而不只检查 manifest 静态路径。

## P1：继续清理剩余触发词冲突

### 问题

`doctor.js` 当前仍有 `14` 个 `HIGH_TRIGGER_COLLISION` warning，主要集中在 PM/planning 类 Skill：

- `brainstorm-experiments-existing`
- `brainstorm-experiments-new`
- `brainstorm-ideas-existing`
- `brainstorm-ideas-new`
- `identify-assumptions-existing`
- `identify-assumptions-new`
- `positioning-ideas`
- `competitor-analysis`
- `swot-analysis`
- `interview-script`
- `summarize-interview`
- `summarize-meeting`
- `prioritize-assumptions`
- `prioritize-features`

### 建议

对 planning 类 Skill 触发词做二次清理：

- 删除共享泛词：`brainstorm`、`ideas`、`analysis`、`summarize` 等如果无法区分意图，降低权重或移出 triggers。
- 增加动作专属词：
  - experiments: `experiment design`、`hypothesis test`、`A/B test plan`
  - ideas: `ideation`、`concept options`、`solution ideas`
  - assumptions: `assumption mapping`、`riskiest assumption`
  - positioning: `positioning statement`、`category narrative`
  - summarize-interview: `interview transcript`、`customer quote`
  - summarize-meeting: `meeting notes`、`action items`
- 对 existing/new 成对 Skill 使用方向性词：
  - existing: `existing`、`current`、`already launched`、`已有`
  - new: `new`、`from scratch`、`greenfield`、`新建`

### 验收

```powershell
node scripts\doctor.js
```

预期：`HIGH_TRIGGER_COLLISION` warning 继续下降，理想值为 0。

## P2：继续修正文档编码和乱码

### 问题

当前通过命令读取 `SKILL.md`、`AGENTS.md`、`README.md` 等核心文档时，中文内容仍呈现乱码。这会影响 Agent 直接阅读说明时的理解质量。

### 建议

优先重写这些核心文档为干净 UTF-8：

- `SKILL.md`
- `AGENTS.md`
- `README.md`
- `router/CATEGORY_INDEX.md`
- `router/SKILL_RANKINGS.md`
- `router/SKILL_POLICY.md`
- `router/ROUTING_CONFIDENCE.md`

建议使用简洁中文或中英双语，但确保文件字节编码为 UTF-8，无重复 frontmatter，无乱码。

### 验收

```powershell
Get-Content -Raw -LiteralPath SKILL.md
Get-Content -Raw -LiteralPath AGENTS.md
Get-Content -Raw -LiteralPath README.md
```

预期：中文正常显示，且 Agent 可直接理解。

## P2：清理剩余旧路径文档

### 问题

仍可检索到旧结构路径，例如：

```text
CONTRIBUTING.md: internal-skills/_super-skill/CATEGORY_INDEX.md
docs/zh-usage.md: internal-skills/_super-skill/INSTRUCTION.md
docs/zh-usage.md: SUPER_SKILL_ROOT = skills
```

这些不一定影响运行，但会误导维护者。

### 建议

将旧路径统一替换为当前结构：

```text
SKILL.md
router/CATEGORY_INDEX.md
router/SKILL_RANKINGS.md
internal-skills/<category>/<skill>/INSTRUCTION.md
```

### 验收

```powershell
rg -n "internal-skills[/\\]_super-skill|SUPER_SKILL_ROOT = skills|THIS_SKILL_DIR/skills" -S .
```

预期：除迁移历史说明或本建议文档外，不再出现旧结构路径。

## 更新后的推荐优先级

1. 先修 `SKILL.md` frontmatter，避免 Codex 元数据解析异常。
2. 修 `route.js` 中 `grammar-check` 的 hardcoded 坏路径。
3. 统一 `route.js` 的 `auxiliary`、`filesToRead`、`absoluteAuxiliaryPaths` 语义。
4. 给 `doctor.js` 增加 route smoke cases 和路径存在性检查。
5. 继续降低 14 个触发词冲突 warning。
6. 最后统一核心文档编码，清理旧路径说明。

## 第二轮最终验收命令

```powershell
Get-ChildItem -Recurse -Force . -Filter SKILL.md | Select-Object FullName
node scripts\doctor.js
node scripts\route.js --query "修复 React 页面按钮样式 bug" --workspace "."
node scripts\route.js --query "review backend auth API security" --workspace "."
node scripts\route.js --query "给论文实验结果写 rebuttal" --workspace "."
node scripts\route.js --query "把需求拆成 GitHub issues" --workspace "."
node scripts\route.js --query "配置 nginx 反向代理和 https 证书" --workspace "."
```

最终目标：

- 根目录 `SKILL.md` frontmatter 干净、唯一、可解析。
- `doctor.js` PASS，且能检查 route 输出路径。
- 所有 route 样例输出 JSON 可解析，路径真实存在。
- manifest 与 route 输出不再出现路径不一致。
- 核心文档无乱码、无旧入口路径误导。

---

## 第三轮复查追加建议

复查时间：2026-07-02

### 当前状态

本轮复查确认功能层面已经比较稳定：

- `node scripts\doctor.js` 当前 PASS，且 warning 为 0。
- 全仓库只剩根目录一个 `SKILL.md`。
- `skills-manifest.json` 当前共有 `141` 条记录，无重复 id、无无效路径、无 `public` visibility 泄露、无必填字段缺失。
- 5 个 route smoke 样例均可输出稳定 JSON，且输出路径真实存在：
  - `修复 React 页面按钮样式 bug`
  - `review backend auth API security`
  - `给论文实验结果写 rebuttal`
  - `把需求拆成 GitHub issues`
  - `配置 nginx 反向代理和 https 证书`
- `给论文实验结果写 rebuttal` 的 `grammar-check` 实际输出路径已经修正为：
  `internal-skills/engineering/planning/pm-skills/grammar-check/INSTRUCTION.md`
- 中置信度样例 `把需求拆成 GitHub issues` 的 `auxiliary`、`filesToRead`、`absoluteAuxiliaryPaths` 已保持一致。

剩余问题主要是工程卫生、文档可读性和分发同步。

## P0：修复 SKILL.md frontmatter 粘连

### 问题

根目录 `SKILL.md` 第 3 行仍然类似：

```yaml
description: ...---
```

也就是结束 `---` 粘在 `description` 行尾。虽然重复 frontmatter 已经去掉，但 YAML frontmatter 仍不标准，严格解析器可能无法识别结束边界。

### 建议

改为标准三段式：

```yaml
---
name: super-skill-router
description: Global entrypoint for Super Skill Router. MUST BE TRIGGERED for ANY coding, development, bug fixing, refactoring, code review, UI/UX design, or architecture task.
---
```

正文从下一行开始。

### 验收

```powershell
Get-Content -LiteralPath SKILL.md -TotalCount 6
```

预期：

- 第 1 行是 `---`。
- 第 4 行是单独的 `---`。
- 不存在 `description:.*---`。

也可以运行：

```powershell
rg -n "description:.*---" SKILL.md
```

预期：无输出。

## P0：同步全局安装目录

### 问题

当前项目内优化后，实际 Codex 触发的全局 Skill 文件可能仍未同步。

检查路径：

```text
C:\Users\a1028\.codex\skills\super-skill-router\SKILL.md
```

该文件仍可能存在 frontmatter 粘连和乱码。若全局目录未同步，用户实际触发的 `super-skill-router` 仍是旧状态。

### 建议

在项目内修复完成后，执行一次明确的同步流程，将这些内容同步到全局目录：

- `SKILL.md`
- `router/`
- `internal-skills/`
- `scripts/`
- `skills-manifest.json`
- `config.json` 如确实需要

同步后再次读取全局 `SKILL.md` 验证。

### 验收

```powershell
Get-Content -LiteralPath "C:\Users\a1028\.codex\skills\super-skill-router\SKILL.md" -TotalCount 6
```

预期：全局文件与项目内修复后的 frontmatter 一致。

## P1：重写核心文档为干净 UTF-8

### 问题

以下核心文档读取时仍显示大量乱码，影响 Agent 直接阅读说明：

- `SKILL.md`
- `AGENTS.md`
- `README.md`
- `router/CATEGORY_INDEX.md`
- `router/SKILL_POLICY.md`
- `router/ROUTER.md`

功能路由虽然已经能跑，但这些文档是 Agent 的人工/半自动决策依据，乱码会降低可靠性。

### 建议

优先重写核心入口和策略文档，保持短小、明确、UTF-8：

1. `SKILL.md`：只保留入口说明、路径解析、route.js 使用方式、router 文档清单。
2. `AGENTS.md`：只保留当前真实入口和路由原则。
3. `README.md`：更新为当前目录结构和最小使用说明。
4. `router/CATEGORY_INDEX.md`：保留分类、触发条件、对应 `CATEGORY_TAG.md` 路径。
5. `router/SKILL_POLICY.md`：保留懒加载、更新/安装确认、辅助 Skill 规则。
6. `router/ROUTER.md`：保留机器可执行路由流程，删除旧结构。

### 验收

```powershell
Get-Content -Raw -LiteralPath SKILL.md
Get-Content -Raw -LiteralPath AGENTS.md
Get-Content -Raw -LiteralPath router\ROUTER.md
```

预期：中文正常显示，无明显 `锛`、`銆`、`鐨`、`璺` 等 mojibake 信号。

## P1：修正 router/ROUTER.md 中的旧结构说明

### 问题

`router/ROUTER.md` 中仍有旧结构：

```text
SUPER_SKILL_ROOT = skills
BUSINESS_SKILL_ROOT = skills
node <SUPER_SKILL_ROOT>/../scripts/route.js
```

当前真实结构应是：

```text
SUPER_SKILL_ROOT = <repo-root>
BUSINESS_SKILL_ROOT = <repo-root>/internal-skills
node <SUPER_SKILL_ROOT>/scripts/route.js
```

### 建议

将 `router/ROUTER.md` 中所有旧路径统一替换为当前结构。特别注意：

- `SUPER_SKILL_ROOT` 不再指向 `skills`。
- `BUSINESS_SKILL_ROOT` 默认指向 `internal-skills`。
- `route.js` 路径不应再使用 `../scripts`。
- 完整 Skill 文件不再叫 `SKILL.md`，应指向 `INSTRUCTION.md`。

### 验收

```powershell
rg -n "SUPER_SKILL_ROOT = skills|BUSINESS_SKILL_ROOT = skills|\.\./scripts/route\.js|完整.*SKILL.md" router\ROUTER.md
```

预期：无旧结构命中。

## P1：清理 route.js 中的死代码旧路径

### 问题

虽然实际输出已正确，但静态搜索仍能在 `scripts/route.js` 中找到：

```text
internal-skills/document/grammar-check/INSTRUCTION.md
```

这说明旧的 hardcoded 分支或死代码仍在文件中。当前运行结果可能被后续修正逻辑覆盖，但这类残留会增加维护风险。

### 建议

删除旧路径硬编码，统一通过 manifest id 查找路径：

```text
grammar-check -> skills-manifest.json -> instructionPath
```

不要在 route 特殊分支中手写 `internal-skills/<category>/<skill>/INSTRUCTION.md`。

### 验收

```powershell
rg -n "document/grammar-check|internal-skills/document/grammar-check" scripts\route.js
node scripts\route.js --query "给论文实验结果写 rebuttal" --workspace "."
```

预期：

- 静态搜索无旧路径残留。
- route 输出仍指向真实 grammar-check 路径。

## P2：整理最终提交前的工作树

### 问题

`git status --short` 显示这是一次较大的迁移：旧 `skills/` 下大量删除，新 `internal-skills/`、`router/`、`skills-manifest.json` 等大量新增。还出现了 `.codegraph/daemon.pid` 这类运行时文件。

### 建议

提交前做一次分组确认：

- 应提交：`SKILL.md`、`AGENTS.md`、`README.md`、`router/`、`internal-skills/`、`scripts/`、`skills-manifest.json`、必要 docs/examples 更新。
- 不应提交：`.codegraph/daemon.pid`、本地缓存、临时输出、无关运行产物。
- 如果旧 `skills/` 删除是迁移目标，提交说明中明确写明“子 Skill 从 `skills/` 迁移到 `internal-skills/`，避免 Codex UI 暴露”。

### 验收

```powershell
git status --short
```

预期：待提交文件都能归类解释，没有运行时垃圾文件。

## P2：让 doctor 覆盖 frontmatter 和旧路径静态检查

### 问题

`doctor.js` 当前已经能检查 route smoke cases，并且 PASS。但它没有捕获 `SKILL.md` frontmatter 粘连，也没有把 `router/ROUTER.md` 旧结构作为 warning/error。

### 建议

给 `doctor.js` 增加检查项：

- 根 `SKILL.md` frontmatter 必须完整闭合。
- 不允许出现 `description:.*---`。
- 不允许核心 router 文档出现 `SUPER_SKILL_ROOT = skills`。
- 不允许 `scripts/route.js` 中残留 `internal-skills/document/grammar-check`。
- 核心文档可选增加 mojibake 信号 warning。

### 验收

```powershell
node scripts\doctor.js
```

预期：上述问题在未修复时能被 doctor 报告，修复后 PASS 且 0 warning。

## 第三轮推荐优先级

1. 修 `SKILL.md` frontmatter 粘连。
2. 同步全局 `C:\Users\a1028\.codex\skills\super-skill-router`。
3. 重写核心文档为干净 UTF-8。
4. 修 `router/ROUTER.md` 旧结构说明。
5. 清理 `scripts/route.js` 中的旧 grammar-check 死代码。
6. 扩展 `doctor.js` 覆盖 frontmatter、旧路径和死代码检查。
7. 最终整理 git 工作树，排除运行时文件。

## 第三轮最终验收命令

```powershell
Get-Content -LiteralPath SKILL.md -TotalCount 6
Get-Content -LiteralPath "C:\Users\a1028\.codex\skills\super-skill-router\SKILL.md" -TotalCount 6
node scripts\doctor.js
rg -n "description:.*---|SUPER_SKILL_ROOT = skills|BUSINESS_SKILL_ROOT = skills|internal-skills/document/grammar-check" -S SKILL.md router scripts
git status --short
```

最终目标：

- 项目内和全局安装的 `SKILL.md` frontmatter 都标准可解析。
- `doctor.js` PASS 且 0 warning。
- route smoke 样例继续全部通过。
- 核心文档无乱码、无旧结构路径。
- `route.js` 无旧路径死代码。
- 工作树中没有无关运行产物。
